export interface Scope {
    prefix: string;
    content: string;
    suffix: string;
    offset: number;
    length: number;
}
export declare class ScopeHelper {
    getStatements(content: string): string[];
    getScopedList(separator: string, content: string, includeSeparatorInSplits?: boolean): string[];
    getCurlyScopes(content: string): Scope[];
    getGenericTypeScopes(content: string): Scope[];
    enumerateRelevantCodeCharacterRegions(content: string, enumerator?: (stringSoFar: string, character: string) => void): string;
    private getScopes;
    private stringEndsWith;
}
